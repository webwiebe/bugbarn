apiVersion: v1
kind: Service
metadata:
  name: bugbarn-web
spec:
  selector:
    app.kubernetes.io/name: bugbarn-web
    app.kubernetes.io/environment: staging
  ports:
    - name: http
      port: 8080
      targetPort: http
