apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bugbarn
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  ingressClassName: traefik
  rules:
    - host: bugbarn.test.wiebe.xyz
      http:
        paths:
          - path: /setup
            pathType: Prefix
            backend:
              service:
                name: bugbarn
                port:
                  name: http
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: bugbarn
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bugbarn-web
                port:
                  name: http
  tls:
    - hosts:
        - bugbarn.test.wiebe.xyz
