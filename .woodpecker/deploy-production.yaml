# Production deploy — AUTOMATIC on a vX.Y.Z tag, gated on the staging deploy.
# Runs in the same tag pipeline as release.yaml and `depends_on` it, so prod
# only deploys after staging has rolled out successfully. Fully hands-off:
# push to main -> auto patch-tag -> staging -> production (no manual step).
# Preflight (image must exist in GHCR) + rollback-on-failure remain the safety net.
# NB: local backend = one shell per `commands` entry, and Woodpecker pre-
# substitutes braced variables; so each step is ONE block and all vars use $VAR
# (no braces). Avoid brace-vars even in comments — Woodpecker parses them.
when:
  - event: tag
    ref: refs/tags/v*

depends_on:
  - release

labels:
  type: build

steps:
  - name: preflight
    image: bash
    environment:
      GHCR_USER: webwiebe
      GHCR_TOKEN:
        from_secret: ghcr_token
      SERVICE_IMAGE: ghcr.io/webwiebe/bugbarn/service
      WEB_IMAGE: ghcr.io/webwiebe/bugbarn/web
    commands:
      - |
        set -euo pipefail
        VERSION="$CI_COMMIT_TAG"
        test -n "$VERSION" || { echo "ERROR: no tag on this pipeline"; exit 1; }
        DOCKER_CONFIG_DIR="$(mktemp -d)"
        printf '{"auths":{"ghcr.io":{"auth":"%s"}}}\n' "$(printf '%s:%s' "$GHCR_USER" "$GHCR_TOKEN" | base64 | tr -d '\n')" > "$DOCKER_CONFIG_DIR/config.json"
        export DOCKER_CONFIG="$DOCKER_CONFIG_DIR"
        docker manifest inspect "$SERVICE_IMAGE:$VERSION" >/dev/null || { echo "ERROR: service $VERSION not in GHCR"; exit 1; }
        docker manifest inspect "$WEB_IMAGE:$VERSION" >/dev/null || { echo "ERROR: web $VERSION not in GHCR"; exit 1; }
        SVC_DIGEST="$(docker buildx imagetools inspect "$SERVICE_IMAGE:$VERSION" --format '{{.Manifest.Digest}}')"
        WEB_DIGEST="$(docker buildx imagetools inspect "$WEB_IMAGE:$VERSION" --format '{{.Manifest.Digest}}')"
        printf 'SERVICE_DIGEST=%s\nWEB_DIGEST=%s\n' "$SVC_DIGEST" "$WEB_DIGEST" > .digests
        echo "Verified $VERSION service@$SVC_DIGEST web@$WEB_DIGEST"

  - name: deploy
    image: bash
    environment:
      SERVICE_IMAGE: ghcr.io/webwiebe/bugbarn/service
      WEB_IMAGE: ghcr.io/webwiebe/bugbarn/web
      K3S_HOST: layer7.wiebe.xyz
      K3S_USER: deployer
      NAMESPACE: bugbarn-production
      SOPS_AGE_KEY:
        from_secret: sops_age_key_production
      GHCR_PULL_PAT:
        from_secret: ghcr_pull_pat
    commands:
      - |
        set -euo pipefail
        . ./.digests
        test -n "$SERVICE_DIGEST" && test -n "$WEB_DIGEST" || { echo "ERROR: missing digests"; exit 1; }
        SSH="ssh -o StrictHostKeyChecking=no"
        $SSH "$K3S_USER@$K3S_HOST" "rm -rf /tmp/bugbarn-deploy && mkdir -p /tmp/bugbarn-deploy"
        # Snapshot what production is running RIGHT NOW, before we touch it, so
        # rollback can restore these exact images instead of using `rollout
        # undo`. Undo is wrong here: the deploy below does `apply -k` (which
        # sets the kustomization's :local placeholder) and only then `set
        # image`, so every deploy leaves a :local revision immediately behind
        # the good one — undo lands on it and production cannot pull.
        # The file is named for this pipeline, so it exists only if THIS
        # deploy actually started; rollback keys off that.
        $SSH "$K3S_USER@$K3S_HOST" "
          set -euo pipefail
          COL='custom-columns=I:.spec.template.spec.containers[0].image'
          W=\$(kubectl -n $NAMESPACE get deploy bugbarn-writer -o \"\$COL\" --no-headers)
          R=\$(kubectl -n $NAMESPACE get deploy bugbarn-reader -o \"\$COL\" --no-headers)
          B=\$(kubectl -n $NAMESPACE get deploy bugbarn-web -o \"\$COL\" --no-headers)
          printf 'PREV_WRITER=%s\nPREV_READER=%s\nPREV_WEB=%s\n' \"\$W\" \"\$R\" \"\$B\" > /tmp/bugbarn-prev-images-$CI_PIPELINE_NUMBER
          cat /tmp/bugbarn-prev-images-$CI_PIPELINE_NUMBER
        "
        scp -o StrictHostKeyChecking=no -r deploy "$K3S_USER@$K3S_HOST:/tmp/bugbarn-deploy/deploy"
        $SSH "$K3S_USER@$K3S_HOST" "kubectl apply -f /tmp/bugbarn-deploy/deploy/k8s/production/namespace.yaml"
        sops -d deploy/k8s/production/secret.yaml | $SSH "$K3S_USER@$K3S_HOST" "kubectl apply -f -"
        sops -d deploy/k8s/production/smtp-secret.yaml | $SSH "$K3S_USER@$K3S_HOST" "kubectl apply -f -"
        $SSH "$K3S_USER@$K3S_HOST" "kubectl create secret docker-registry ghcr-secret --docker-server=ghcr.io --docker-username='webwiebe' --docker-password='$GHCR_PULL_PAT' --namespace=$NAMESPACE --dry-run=client -o yaml | kubectl apply -f -"
        $SSH "$K3S_USER@$K3S_HOST" "
          set -euo pipefail
          kubectl apply -k /tmp/bugbarn-deploy/deploy/k8s/production
          kubectl -n $NAMESPACE delete deployment/bugbarn --ignore-not-found=true
          kubectl -n $NAMESPACE set image deployment/bugbarn-writer bugbarn=$SERVICE_IMAGE@$SERVICE_DIGEST
          kubectl -n $NAMESPACE set image deployment/bugbarn-reader bugbarn=$SERVICE_IMAGE@$SERVICE_DIGEST
          kubectl -n $NAMESPACE set image deployment/bugbarn-web bugbarn-web=$WEB_IMAGE@$WEB_DIGEST
          # The CronJob runs the service image too. Without this it keeps the
          # kustomization's placeholder :local tag and every run dies in
          # ImagePullBackOff — the DR snapshot silently never happens.
          kubectl -n $NAMESPACE set image cronjob/bugbarn-settings-snapshot snapshot=$SERVICE_IMAGE@$SERVICE_DIGEST
          # 600s, not 180s: the writer runs schema migrations at startup, and a
          # migration over a multi-gigabyte database can take minutes (the
          # event_facets index in 00011 took over three minutes on staging's
          # 9GB file). The deployment's startupProbe already budgets 10m for
          # exactly this; waiting only 3m here made the two disagree, so a
          # perfectly healthy slow rollout was reported as a failed deploy —
          # and on production that fires the rollback. Keep this in step with
          # the startupProbe budget in the writer deployment manifests.
          kubectl -n $NAMESPACE rollout status deployment/bugbarn-writer --timeout=600s
          kubectl -n $NAMESPACE rollout status deployment/bugbarn-reader --timeout=180s
          kubectl -n $NAMESPACE rollout status deployment/bugbarn-web --timeout=180s
        "

  - name: rollback-on-failure
    image: bash
    when:
      - status: [ failure ]
    environment:
      K3S_HOST: layer7.wiebe.xyz
      K3S_USER: deployer
      NAMESPACE: bugbarn-production
    commands:
      - |
        ssh -o StrictHostKeyChecking=no "$K3S_USER@$K3S_HOST" "
          SNAP=/tmp/bugbarn-prev-images-$CI_PIPELINE_NUMBER
          # No snapshot means the deploy step never ran — a preflight failure,
          # say. Production is untouched and healthy, so rolling it back would
          # CAUSE the outage rather than undo one. That is exactly what took
          # prod down on v0.236.156: preflight died on a missing docker binary,
          # deploy was skipped, and the blanket rollback still fired.
          test -s \"\$SNAP\" || { echo 'no pre-deploy snapshot for this pipeline; deploy never started, nothing to roll back'; exit 0; }
          . \"\$SNAP\"
          # Never restore the kustomization placeholder: it is unpullable.
          for IMG in \"\$PREV_WRITER\" \"\$PREV_READER\" \"\$PREV_WEB\"; do
            case \"\$IMG\" in
              ''|*:local) echo \"ERROR: refusing to roll back to unusable image '\$IMG'\"; exit 1 ;;
            esac
          done
          kubectl -n $NAMESPACE set image deployment/bugbarn-writer bugbarn=\"\$PREV_WRITER\"
          kubectl -n $NAMESPACE set image deployment/bugbarn-reader bugbarn=\"\$PREV_READER\"
          kubectl -n $NAMESPACE set image deployment/bugbarn-web bugbarn-web=\"\$PREV_WEB\"
          # Same 600s budget as the forward deploy. The rolled-back image opens
          # the same (already migrated) database, and reporting a rollback as
          # failed because it took over 3m helps nobody.
          kubectl -n $NAMESPACE rollout status deployment/bugbarn-writer --timeout=600s
          echo \"Rolled back to \$PREV_WRITER\"
        "

  - name: release-marker
    image: bash
    environment:
      BUGBARN_API_KEY:
        from_secret: bugbarn_api_key
      BUGBARN_ENDPOINT: https://bugbarn.wiebe.xyz
    commands:
      - |
        curl -sf -X POST "$BUGBARN_ENDPOINT/api/v1/releases" \
          -H "Content-Type: application/json" \
          -H "X-BugBarn-Api-Key: $BUGBARN_API_KEY" \
          -H "X-BugBarn-Project: bugbarn-service" \
          -d "{\"name\":\"$CI_COMMIT_TAG\",\"environment\":\"production\",\"version\":\"$CI_COMMIT_TAG\",\"commitSha\":\"$CI_COMMIT_SHA\",\"url\":\"$CI_REPO_URL/releases/tag/$CI_COMMIT_TAG\",\"notes\":\"Deployed by Woodpecker #$CI_PIPELINE_NUMBER\"}" \
          || echo "WARN: BugBarn release marker failed"
        echo "Production deploy complete: $CI_COMMIT_TAG -> https://bugbarn.wiebe.xyz"
